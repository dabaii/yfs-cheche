因为开启了VPN网络导致 Python 的 SSL 在通过你的 VPN 代理建立 HTTPS 连接时，没有拿到正确的目标域名（server_hostname），导致 SSL 校验失败。

首先尝试正常安装。

如果无法正常安装，请尝试其他解决方案。

解决方案1：
``` bash
pip install 包名 --proxy http://127.0.0.1:10809
```


解决方案2请在安装使用中国大陆镜像：

``` bash
pip install 包名 -i https://pypi.tuna.tsinghua.edu.cn/simple
```